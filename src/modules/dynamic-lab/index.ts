/**
 * Dynamic Lab Module - Personal voice channel creation
 *
 * Users join a "Get Lab Here" channel and the bot creates a personal
 * lab (voice channel) for them with full control via button-based UI.
 */

import { DynamicLabModule } from './module.js';

export default new DynamicLabModule();
export { DynamicLabModule };
