/**
 * Role Management Module - Reaction roles for Discord servers
 *
 * Create messages that users can react to in order to get roles.
 * Managed via an interactive panel with buttons and dropdowns.
 */

import { RoleManagementModule } from './module.js';

export default new RoleManagementModule();
export { RoleManagementModule };
