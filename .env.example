# ===========================================
# Discord Bot Configuration (REQUIRED)
# ===========================================

# Your Discord bot token
# Get one at: https://discord.com/developers/applications
BOT_TOKEN=your_bot_token_here

# Your Discord application's client ID
# Found on the application's General Information page
CLIENT_ID=your_client_id_here

# ===========================================
# Database Configuration (REQUIRED)
# ===========================================

# MySQL connection string
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=mysql://root:password@localhost:3306/quadslab_bot

# ===========================================
# Development Settings (OPTIONAL)
# ===========================================

# Your Discord server ID for instant slash command updates
# Without this, global commands take up to 1 hour to propagate
# Find it by enabling Developer Mode in Discord, then right-click your server
DEV_GUILD_ID=

# Environment mode
# Options: development | production
NODE_ENV=development

# Log verbosity
# Options: debug | info | warn | error
LOG_LEVEL=debug

# ===========================================
# Bot Owner Configuration (OPTIONAL)
# ===========================================

# Comma-separated Discord user IDs that have owner-level bot access
# These users can run admin commands regardless of server permissions
BOT_OWNER_IDS=

# ===========================================
# Website Integration (OPTIONAL)
# ===========================================
# Only needed if using the website-integration module

# Base URL of your website
WEBSITE_URL=
# Shared secret for authenticating webhook requests
WEBSITE_WEBHOOK_SECRET=
# Port for the webhook server (default: 3001)
WEBHOOK_SERVER_PORT=3001

# ===========================================
# Ngrok Tunnel (OPTIONAL)
# ===========================================
# Only needed for exposing the webhook server during local development

# Set to true to automatically start an ngrok tunnel
NGROK_ENABLED=false
# Your ngrok auth token (get one at https://ngrok.com)
NGROK_AUTH_TOKEN=
# Ngrok region: us, eu, ap, au, sa, jp, in
NGROK_REGION=us

# ===========================================
# Docker Settings (OPTIONAL)
# ===========================================
# Only needed when running with docker-compose

# MySQL root password for the Docker container
MYSQL_ROOT_PASSWORD=rootpassword
# MySQL user password for the bot's database user
MYSQL_PASSWORD=botpassword
# Webhook port inside Docker (to avoid conflicts with local dev)
DOCKER_WEBHOOK_PORT=3050
